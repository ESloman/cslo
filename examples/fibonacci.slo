
# the number to work the fibonacci value of
var number = 20;

# define our fibonacci function
func fibonacci(n) {
  if (n <= 1) {
    return n;
  }
  return fibonacci(n - 2) + fibonacci(n - 1);
}

print(fibonacci(number));

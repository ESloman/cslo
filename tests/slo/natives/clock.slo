var start = clock();

for (var c = 0; c < 10000; c = c + 1) {
    var x = 1000;
    var y = 1000 * 1000;
    var z = y * x * x * (c + 1);
    println(z);
}

var end = clock();
println("Took: ");
println(end - start);

var map = {"a": 1, "b": 2, "c": 3};
println(map has "a");  # true
println(map has "b");  # true
println(map has "d");  # false

println(map has not "a");  # false
println(map has not "b");  # false
println(map has not "d");  # true

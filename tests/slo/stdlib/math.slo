
import math;

# ceil and floor
println("Printing ceil and floor stuff");
for (var x = 0; x < 10; x = x + 1.1) {
    println("Testing with ", x, "; ceil: ", math.ceil(x), ", floor: ", math.floor(x));
}

# sqrt
println(math.sqrt(9));  # 3

# sin
println(math.sin(1));

# cos
println(math.cos(1));

# tan
println(math.tan(1));

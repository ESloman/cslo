import json;

var file = open("tests/slo/stdlib/json.json");
var content = file.read();
file.close();

var s = {"a": 1, "b": [2, 3]};
var obj = json.loads(content);
println(obj);
for (var key in obj) {
    println(key, ": ", obj[key]);
}

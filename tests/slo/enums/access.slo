enum Colours {
    BLACK,
    BLUE,
    YELLOW,
    ORANGE
}

println(Colours.BLACK);        # 0
println(Colours.YELLOW == 2);  # true

var yellow = Colours.YELLOW;
println(yellow);               # 2

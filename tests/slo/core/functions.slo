func echo(x) {
    println(x);
    println(x + 10);
}

println("Start");
for (var i = 0; i < 25; i = i + 1) {
    println(i);
}

println("End");

echo(59);

func sloman() {
    return 5 + 6;
}

func sloman2(x, y, z) {
    return x + y + z;
}

println(sloman);
println(sloman2);

println(sloman());
println(sloman2(1, 5, 9));
